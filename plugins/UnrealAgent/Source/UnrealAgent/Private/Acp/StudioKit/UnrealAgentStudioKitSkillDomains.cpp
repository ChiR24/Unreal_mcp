#include "Acp/StudioKit/UnrealAgentStudioKitPrivate.h"

namespace UnrealAgentStudioKit
{
    FString MakeWorldBuildingDisciplineSkill()
    {
        return MakeSkillMarkdown(
            TEXT("unreal-world-building-discipline"),
            TEXT("Plan landscape, foliage, PCG, geometry, splines, and World Partition changes with viewport, save, and runtime evidence."),
            TEXT("# Unreal World Building Discipline\n\nUse this before changing Landscapes, heightmaps, landscape materials or layer info assets, foliage types or instances, Procedural Foliage, PCG graphs/components/nodes, spline roads/rivers/fences, Modeling Mode or generated geometry, blocking volumes, world volumes, Data Layers, Runtime Grid, HLOD, World Partition cells, or large environment dressing passes. World-building changes depend on the current map, World Partition and Data Layer ownership, landscape actor/material/layer state, foliage and PCG source assets, collision/navmesh impact, streaming bounds, viewport visibility, and performance budget.\n\nRequired discipline:\n- Run /unreal-tool-inventory, then inspect the current map/editor world, World Partition state, Data Layers, Runtime Grid, HLOD or streaming settings, selected actors, Landscape actor and layer info assets, landscape material and painted layers, foliage types and instance counts, PCG graph/component/nodes/execution state, spline actors/components, generated geometry targets, collision/navmesh implications, dirty packages, viewport state, and recent landscape, foliage, PCG, geometry, Message Log, and Output Log entries.\n- State the world-building route: desired environment result, owning map/level/Data Layer, affected landscape/foliage/PCG/geometry/spline assets, collision/navigation/streaming/performance assumptions, narrow build_environment/manage_pcg/manage_geometry/manage_level_structure MCP route plus any needed asset/material/actor/editor route, validation, and rollback.\n- Keep terrain sculpting, landscape material/layer painting, foliage type configuration, foliage instance painting/removal, PCG graph authoring and execution, spline meshes, generated meshes, blocking/physics/nav/audio/post-process volumes, Data Layer membership, World Partition streaming, HLOD, and baked runtime evidence distinct. Do not edit or move .uasset or .umap landscape, foliage, PCG, geometry, or world-partition assets with local filesystem tools.\n- After mutation, save touched map, external actor packages, landscape/foliage/PCG/geometry/material assets, refresh inspect, rebuild or refresh navigation/HLOD/PCG generation when required, capture a viewport screenshot when visual layout matters, and review landscape, foliage, PCG, geometry, Message Log, and Output Log errors.\n- Validate in editor or PIE: terrain shape and layer paint are visible, foliage density/culling/collision match intent, PCG outputs generate in the expected bounds, geometry transforms/collision are correct, Data Layer and World Partition ownership is intentional, navmesh/pathing and streaming still work, performance/scalability impact is acceptable for the scope, logs stay clean, and persisted map/external-actor state is proven after preview sessions.\n- If viewport appearance, terrain/layer state, foliage instances, PCG generation, geometry collision, Data Layer ownership, World Partition streaming, navigation, or save state cannot be observed, state world-building behavior is unverified and do not claim the environment loop works.\n\nIf MCP cannot inspect landscape/foliage/PCG/geometry/world state or run editor/PIE validation, limit work to source/config/docs/log analysis and report the missing environment evidence."));
    }

    FString MakeGameplayInputDisciplineSkill()
    {
        return MakeSkillMarkdown(
            TEXT("unreal-gameplay-input-discipline"),
            TEXT("Plan gameplay framework and Enhanced Input changes with possession, binding, compile, save, and PIE evidence."),
            TEXT("# Unreal Gameplay And Input Discipline\n\nUse this before changing GameMode, GameState, PlayerController, PlayerState, Pawn, Character, camera, HUD/UI, input assets, input bindings, Enhanced Input Actions, Mapping Contexts, inventory/combat/interaction entry points, or first-playable controls. Unreal gameplay behavior depends on framework ownership and runtime possession; Enhanced Input behavior depends on Input Actions, Mapping Contexts, priority, triggers, modifiers, and runtime assignment.\n\nRequired discipline:\n- Run /unreal-tool-inventory, then inspect project maps/settings, active GameMode or World Settings override, Default Pawn/Class, PlayerController, HUD, GameState, PlayerState, selected Pawn/Character Blueprint or C++ class, input assets, mapping contexts, dirty packages, and recent logs.\n- State the playable route: user-facing action, framework owner, Pawn/Controller possession path, camera/HUD feedback, Input Action and Mapping Context names, priority/triggers/modifiers, network authority if relevant, narrow MCP parent tool/action, validation, and rollback.\n- Keep GameMode rules, PlayerController input/camera ownership, Pawn movement, Character movement, HUD/UI feedback, GameState/PlayerState replication, SaveGame persistence, and ability/combat/inventory entry points distinct.\n- After mutation, compile affected Blueprints or C++, save touched assets/config/maps, refresh inspect, and review Blueprint compiler, Message Log, Output Log, and input-related errors.\n- Validate in PIE: correct pawn is possessed, expected input events fire, camera and HUD respond, gameplay state changes, logs stay clean, and multiplayer/client authority assumptions are tested when relevant.\n- If input or possession cannot be observed, state gameplay behavior is unverified and do not claim the playable loop works.\n\nIf MCP cannot inspect or run PIE, limit work to source/config/docs/log analysis and report the missing runtime evidence."));
    }

    FString MakeAnimationPhysicsDisciplineSkill()
    {
        return MakeSkillMarkdown(
            TEXT("unreal-animation-physics-discipline"),
            TEXT("Plan animation and physics changes with asset ownership, collision, compile, save, and PIE evidence."),
            TEXT("# Unreal Animation And Physics Discipline\n\nUse this before changing Skeletal Mesh, Skeleton, Animation Blueprint, AnimGraph, State Machine, Blend Space, Aim Offset, Animation Montage, Animation Notify, Control Rig, IK Rig, IK Retargeter, Physics Asset, Chaos simulation, collision bodies, collision channels, trace channels, constraints, ragdoll, root motion, or character movement-driven animation. Unreal motion depends on consistent ownership between Skeleton, Skeletal Mesh, Animation Blueprint, linked animation assets, Physics Asset, movement component, collision profiles, and runtime gameplay state.\n\nRequired discipline:\n- Run /unreal-tool-inventory, then inspect the Skeletal Mesh, Skeleton, Animation Blueprint, AnimGraph/state machine, Blend Space or Montage, Notify tracks, Control Rig/IK/retarget assets, Physics Asset bodies/constraints, collision profiles/channels, movement component, root-motion settings, dirty packages, and recent animation, physics, Blueprint compiler, Message Log, and Output Log entries.\n- State the motion route: desired pose/movement/simulation change, owning Skeleton/Skeletal Mesh/Animation Blueprint/Physics Asset, affected state/transition/slot/notify/collision body, root-motion and network authority assumptions, narrow MCP parent tool/action, validation, and rollback.\n- Keep animation graph logic, montage playback, notify timing, retargeting, Control Rig, movement component input, Physics Asset bodies, collision channels, constraints, ragdoll/simulation, and replicated movement authority distinct.\n- After mutation, compile affected Animation Blueprints or C++, save touched animation, physics, Blueprint, character, and config assets, refresh inspect, and review animation, physics, Blueprint compiler, Message Log, and Output Log errors.\n- Validate in PIE: the expected animation state or montage plays, transitions and blend weights behave, notifies fire at the expected time, collision or trace responses match intent, physics bodies remain stable, root motion or movement authority is correct, logs stay clean, and multiplayer/server-authority assumptions are tested when relevant.\n- If animation playback, notify timing, collision, physics simulation, or root motion cannot be observed, state motion behavior is unverified and do not claim the character or physics loop works.\n\nIf MCP cannot inspect animation/physics assets or PIE, limit work to source/config/docs/log analysis and report the missing runtime evidence."));
    }

    FString MakeVfxMaterialDisciplineSkill()
    {
        return MakeSkillMarkdown(
            TEXT("unreal-vfx-material-discipline"),
            TEXT("Plan Niagara, VFX, material, and texture changes with asset ownership, shader/effect state, save, screenshot, and PIE evidence."),
            TEXT("# Unreal VFX And Material Discipline\n\nUse this before changing Niagara Systems, emitters, modules, user parameters, particle spawn/update logic, Material graphs, Material Instances, textures, shader settings, decals, post process materials, Lumen/Nanite-facing visuals, effect attachment, pooling, scalability, bounds, or culling. Unreal visual behavior depends on clear ownership between asset package paths, Niagara components, emitters/modules, user parameters, Materials, Material Instances, textures, shader compile state, actor attachment, bounds, scalability, and runtime viewport state.\n\nRequired discipline:\n- Run /unreal-tool-inventory, then inspect the target Actor/component, Niagara System, emitter stack, modules, user parameters, attached sockets/components, Material or Material Instance, texture assets, shader compile state, render settings, bounds/culling, scalability settings, dirty packages, viewport state, screenshots when visual appearance matters, and recent Niagara, material, shader, Message Log, and Output Log entries.\n- State the visual route: desired screen result, owning Actor/component/effect/material, Niagara emitter/module/user parameter, Material/Material Instance/texture assignment, shader or scalability risk, bounds/culling expectation, narrow MCP parent tool/action, validation, and rollback.\n- Keep Niagara asset edits, component spawning/attachment, user parameters, material graph edits, Material Instance overrides, texture import/compression, decal/post-process assignment, shader compilation, scalability/performance, and runtime gameplay triggers distinct.\n- After mutation, compile or refresh affected materials/shaders where applicable, save touched Niagara/material/texture/Actor/level assets, refresh inspect, capture a viewport screenshot when visual output matters, and review Niagara, shader, material, Message Log, and Output Log errors.\n- Validate in PIE: the effect spawns at the expected location and lifetime, user parameters update, material or texture assignment is visible, shader compile state is clean, bounds/culling do not hide the effect, performance impact is acceptable for the scope, logs stay clean, and multiplayer/client visibility assumptions are tested when relevant.\n- If effect spawning, material assignment, shader state, viewport appearance, bounds/culling, or performance cannot be observed, state visual behavior is unverified and do not claim the VFX or material loop works.\n\nIf MCP cannot inspect VFX/material assets, viewport screenshots, or PIE, limit work to source/config/docs/log analysis and report the missing runtime evidence."));
    }

    FString MakeAudioDisciplineSkill()
    {
        return MakeSkillMarkdown(
            TEXT("unreal-audio-discipline"),
            TEXT("Plan Sound Wave, Sound Cue, MetaSound, Audio Component, attenuation, concurrency, and mix changes with runtime evidence."),
            TEXT("# Unreal Audio Discipline\n\nUse this before changing Sound Waves, Sound Cues, MetaSound Sources or Presets, Audio Components, attenuation, concurrency, Submixes, Sound Classes, Sound Mixes, Audio Volumes, reverb, music systems, dialog, ambience, interactive weapon or vehicle sounds, or gameplay-triggered audio. Unreal audio behavior depends on clear ownership between source assets, components, actor/world placement, listener position, attenuation/spatialization, concurrency, routing, Submix/Sound Class mix state, trigger logic, and runtime platform settings.\n\nRequired discipline:\n- Run /unreal-tool-inventory, then inspect the owning Actor/component, Sound Wave, Sound Cue, MetaSound Source or Preset, Audio Component, Sound Attenuation, Sound Concurrency, Submix, Sound Class/Mix, Audio Volume/Reverb, listener/player context, trigger Blueprint or C++ path, dirty packages, and recent audio, Message Log, and Output Log entries.\n- State the audio route: desired player-facing sound result, owning asset/component/actor, trigger path, attenuation/spatialization expectation, concurrency/voice-stealing risk, Submix or mix routing, narrow manage_audio MCP route plus any needed asset/actor/Blueprint route, validation, and rollback.\n- Keep imported source audio, procedural MetaSound DSP graphs, Sound Cue logic, component placement, listener-relative spatialization, concurrency, Submix effects, music/dialog state, and gameplay trigger logic distinct. Do not edit or move .uasset or .umap audio assets with local filesystem tools.\n- After mutation, save touched audio, Blueprint, Actor, level, and config assets, refresh inspect, review audio logs, and use Audio Insights, profiling, or runtime logs when available for voice, Submix, concurrency, or performance claims.\n- Validate in PIE: the sound plays from the expected trigger, attenuation and spatialization respond to listener distance/orientation, volume and mix routing are correct, concurrency behavior matches intent, music/dialog state transitions are audible, logs stay clean, and multiplayer/client listener assumptions are tested when relevant.\n- If audible playback, listener-relative spatialization, mix routing, concurrency, or trigger timing cannot be observed or inspected, state audio behavior is unverified and do not claim the sound loop works.\n\nIf MCP cannot inspect audio assets, Audio Components, or PIE, limit work to source/config/docs/log analysis and report the missing runtime evidence."));
    }

    FString MakeCinematicSequenceDisciplineSkill()
    {
        return MakeSkillMarkdown(
            TEXT("unreal-cinematic-sequence-discipline"),
            TEXT("Plan Sequencer, Level Sequence, camera, shot, keyframe, binding, and Movie Render Queue changes with playback evidence."),
            TEXT("# Unreal Cinematic Sequence Discipline\n\nUse this before changing Level Sequence Assets, Level Sequence Actors, Sequencer tracks, keyframes, bindings, spawnables, possessables, Camera Cut tracks, CineCameraActors, shots, animation/audio tracks, playback settings, restore-state behavior, Movie Render Queue jobs, render presets, or cinematic output paths. Sequencer work depends on the Content Browser Level Sequence Asset plus the Level Sequence Actor binding it to the current Level; render work depends on the correct map, sequence, camera, queue preset, output settings, and plugin availability.\n\nRequired discipline:\n- Run /unreal-tool-inventory, then inspect the current map, Level Sequence Asset, Level Sequence Actor, bound actors, spawnable or possessable ownership, tracks, keyframes, sections, Camera Cut track, CineCameraActors, playback range/rate, restore-state settings, dirty packages, and Sequencer, Movie Render Queue, Message Log, and Output Log entries.\n- State the cinematic route: intended shot or timeline result, owning Level Sequence Asset and Level Sequence Actor, affected tracks/sections/keyframes/bindings/cameras, render or playback target, narrow manage_sequence MCP route plus any needed actor/level/asset/editor route, validation, and rollback.\n- Keep sequence asset edits, level actor bindings, spawnables versus possessables, camera cuts, animated actor transforms, audio tracks, playback settings, render queue presets, output paths, and runtime trigger logic distinct. Do not edit or move .uasset or .umap sequence, camera, or render-preset assets with local filesystem tools.\n- After mutation, save the Level Sequence, Level Sequence Actor/map, camera, render preset, and touched assets, refresh inspect, scrub or play the sequence when possible, check for unresolved bindings, and review Sequencer/Movie Render Queue logs.\n- Validate in editor or PIE: timeline playback reaches the expected frames, camera cuts take control correctly, bound actors animate as intended, audio/cinematic tracks align, restore-state behavior is intentional, Movie Render Queue uses the correct map/sequence/config/output when rendering, and logs stay clean.\n- If sequence playback, camera cuts, actor bindings, render queue configuration, or output frames cannot be observed or inspected, state cinematic behavior is unverified and do not claim the cinematic or render loop works.\n\nIf MCP cannot inspect Sequencer, Level Sequence assets, Movie Render Queue, or PIE/editor playback, limit work to source/config/docs/log analysis and report the missing runtime evidence."));
    }

    FString MakeNetworkingGasDisciplineSkill()
    {
        return MakeSkillMarkdown(
            TEXT("unreal-networking-gas-discipline"),
            TEXT("Plan multiplayer replication, authority, RPC, prediction, and Gameplay Ability System changes with network evidence."),
            TEXT("# Unreal Networking And GAS Discipline\n\nUse this before changing replicated gameplay state, RPCs, ownership, relevancy, dormancy, RepGraph or Iris assumptions, sessions, multiplayer PIE, dedicated-server behavior, Ability System Components, Gameplay Abilities, Attribute Sets, Gameplay Effects, Gameplay Cues, Gameplay Tags, ability input activation, costs, cooldowns, prediction, or server-authoritative combat abilities. Unreal networking depends on server authority, replicated Actor ownership, NetMode/Role context, RPC direction, property replication, and client prediction. GAS depends on a valid Ability System Component owner/avatar, granted abilities, attributes, effects, tags, cues, and prediction windows.\n\nRequired discipline:\n- Run /unreal-tool-inventory, then inspect NetMode, PIE client count, GameMode, GameState, PlayerState, PlayerController, Pawn/Character, replicated Actors/components, owner/role, relevant replicated properties or RPC paths, Ability System Component owner/avatar, Attribute Sets, granted abilities/effects/tags/cues, dirty packages, and networking, GAS, Message Log, and Output Log entries.\n- State the multiplayer route: desired authoritative result, server-owned state, client-visible state, RPC/property replication path, GAS owner/avatar relationship, ability/effect/tag/cue contract, prediction or rollback expectation, narrow manage_networking/manage_gas MCP route plus any needed gameplay/Blueprint/system_control route, validation, and rollback.\n- Keep GameMode-only authority, GameState/PlayerState replicated state, Pawn possession, PlayerController-local UI/input, replicated Actor ownership, RPC direction, movement prediction, ability activation, Gameplay Effect application, Attribute replication, Gameplay Cue presentation, and save/load persistence distinct. Do not edit or move .uasset or .umap networking or GAS assets with local filesystem tools.\n- After mutation, compile affected Blueprints or C++, save touched gameplay, GAS, config, and map assets, refresh inspect, and review networking, GAS, Blueprint compiler, Message Log, and Output Log errors.\n- Validate in multiplayer PIE or the strongest available network check: server authority owns the mutation, clients see replicated state, OnRep/RPC paths fire once and in the expected direction, late join or respawn assumptions are covered when relevant, abilities activate only under valid authority/prediction, costs/cooldowns/effects/tags/cues apply correctly, logs stay clean, and single-player evidence is not used to claim multiplayer correctness.\n- If authority, replicated state, RPC direction, ability prediction, effects, cues, or client/server visibility cannot be observed, state multiplayer or GAS behavior is unverified and do not claim the networked gameplay loop works.\n\nIf MCP cannot inspect networking/GAS state or run multiplayer PIE, limit work to source/config/docs/log analysis and report the missing server/client evidence."));
    }

    FString MakeCharacterSystemsDisciplineSkill()
    {
        return MakeSkillMarkdown(
            TEXT("unreal-character-systems-discipline"),
            TEXT("Plan Character, movement, combat, inventory, equipment, and interaction changes with runtime evidence."),
            TEXT("# Unreal Character Systems Discipline\n\nUse this before changing Character or Pawn classes, Character Movement Component settings, camera boom or camera ownership, capsule/collision, mesh/animation coupling, health/damage, weapons, projectiles, hit traces, targeting, inventory items, equipment slots, pickups, interactable Actors, interfaces, overlap/trace prompts, or first-playable interaction loops. Character systems depend on Gameplay Framework ownership between GameMode, PlayerController, Pawn/Character, PlayerState, HUD/UI, components, data assets, collision channels, input, animation, and network authority.\n\nRequired discipline:\n- Run /unreal-tool-inventory, then inspect the active GameMode or World Settings override, selected Pawn/Character, PlayerController, PlayerState, HUD/UI feedback path, Character Movement Component, capsule/root component, mesh/Animation Blueprint, camera/spring arm, collision channels/profiles, health/damage component, weapon/projectile/combat assets, inventory/equipment data assets, interactable Actor/interface/trace or overlap path, dirty packages, and recent gameplay, collision, Blueprint compiler, Message Log, and Output Log entries.\n- State the character systems route: desired player-facing result, owning Character/Pawn/Controller/component/data asset, input and animation dependencies, collision/damage contract, inventory or equipment state owner, interaction detection path, multiplayer authority assumption, narrow manage_character/manage_combat/manage_inventory/manage_interaction MCP route plus any needed Blueprint/asset/actor/UI route, validation, and rollback.\n- Keep movement tuning, camera behavior, collision profiles, combat traces/projectiles, damage application, health state, inventory data, equipment attachment, interaction prompts, UI feedback, animation notifies, audio/VFX feedback, and replicated authority distinct. Do not edit or move .uasset or .umap character, combat, inventory, or interaction assets with local filesystem tools.\n- After mutation, compile affected Blueprints or C++, save touched character, combat, inventory, interaction, animation, UI, config, and map assets, refresh inspect, and review gameplay, collision, Blueprint compiler, Message Log, and Output Log errors.\n- Validate in PIE: the correct pawn is possessed, movement and camera respond, collision and traces hit intended targets, damage/health state changes once and in the right authority context, inventory/equipment state updates and persists as intended, interaction prompts and activation work, UI/audio/VFX feedback matches gameplay state, logs stay clean, and multiplayer authority/client-visibility assumptions are tested when relevant.\n- If movement, combat hit registration, damage, inventory mutation, interaction activation, UI feedback, or authority cannot be observed, state character-system behavior is unverified and do not claim the playable loop works.\n\nIf MCP cannot inspect character/combat/inventory/interaction state or PIE, limit work to source/config/docs/log analysis and report the missing runtime evidence."));
    }

    FString MakeUiHudDisciplineSkill()
    {
        return MakeSkillMarkdown(
            TEXT("unreal-ui-hud-discipline"),
            TEXT("Plan UMG, HUD, and CommonUI changes with widget ownership, layout, input focus, compile, save, and PIE evidence."),
            TEXT("# Unreal UI And HUD Discipline\n\nUse this before changing HUD classes, UMG Widget Blueprints, UserWidget C++ classes, CommonUI activatable widgets, menus, HUD overlays, input mode, focus handling, viewport-added widgets, UI animations, DPI scaling, anchors, localization text, accessibility metadata, or platform/controller navigation. Unreal UI behavior depends on clear ownership between GameMode/HUD/PlayerController, Widget Blueprint/UserWidget assets, CommonUI layer or viewport ownership, input mode, focus target, Slate/UMG invalidation, and runtime viewport state.\n\nRequired discipline:\n- Run /unreal-tool-inventory, then inspect HUD class, PlayerController, active viewport/PIE state, selected Widget Blueprint or UserWidget class, widget hierarchy, bindings, exposed variables, animations, CommonUI activatable widget stack or viewport add/remove path, input mode, focus target, DPI scale, anchors, localization/accessibility risk, dirty packages, and recent logs.\n- State the UI route: user-facing screen or HUD change, owning HUD/Controller/widget, widget tree or CommonUI layer, input/focus behavior, DPI/layout expectation, localization/accessibility impact, narrow MCP parent tool/action, validation, and rollback.\n- Keep HUD ownership, PlayerController input mode, widget creation/add-to-viewport lifetime, CommonUI activation stack, focus/navigation, data binding, animation, localization, accessibility, and save/load state distinct.\n- After mutation, compile affected Widget Blueprints or C++, save touched UI assets/config/maps, refresh inspect, and review Blueprint compiler, UMG, Slate, CommonUI, Message Log, and Output Log errors.\n- Validate in PIE: expected widget appears in the viewport, layout fits at target DPI/aspect ratios, keyboard/gamepad/mouse/touch focus and navigation work, HUD updates from gameplay state, CommonUI activation/back behavior works when relevant, logs stay clean, and multiplayer/local-player assumptions are tested when relevant.\n- If viewport display, input focus, navigation, or HUD data cannot be observed, state UI behavior is unverified and do not claim the screen or HUD loop works.\n\nIf MCP cannot inspect UI assets, viewport state, or PIE, limit work to source/config/docs/log analysis and report the missing runtime evidence."));
    }

    FString MakeAiNavigationDisciplineSkill()
    {
        return MakeSkillMarkdown(
            TEXT("unreal-ai-navigation-discipline"),
            TEXT("Plan AI behavior and navigation changes with brain, perception, pathing, compile, save, and PIE evidence."),
            TEXT("# Unreal AI And Navigation Discipline\n\nUse this before changing AIController, Behavior Tree, Blackboard, AI Perception, EQS queries, NavMesh, Nav Mesh Bounds Volume, RecastNavMesh settings, patrol routes, chase/flee behavior, pathfinding, crowd movement, or NPC first-playable behavior. Unreal AI behavior depends on clear ownership between the Pawn, AIController, Behavior Tree, Blackboard, Perception, EQS, movement/path following, collision, and generated navigation data.\n\nRequired discipline:\n- Run /unreal-tool-inventory, then inspect the AI Pawn/Character, AIController, Behavior Tree, Blackboard keys, active tasks/services/decorators, Perception senses/stimuli, EQS query assets, Nav Mesh Bounds Volume, RecastNavMesh/build settings, collision, movement component, patrol targets, spawn points, dirty packages, and recent logs.\n- State the AI route: desired behavior, owner for each change, Blackboard key contract, Behavior Tree branch/task/service/decorator, Perception or EQS expectation, NavMesh/pathing requirement, narrow MCP parent tool/action, validation, and rollback.\n- Keep AIController possession, Behavior Tree execution, Blackboard state, Perception stimuli, EQS scoring, Pawn movement/path following, NavMesh generation, animation/combat effects, and replicated authority distinct.\n- After mutation, compile affected Blueprints or C++, save touched Behavior Tree, Blackboard, EQS, Pawn/Controller, level, and config assets, rebuild or refresh navigation if required, refresh inspect, and review Behavior Tree, navigation, AI, Message Log, and Output Log errors.\n- Validate in PIE: AIController possesses the expected pawn, Blackboard keys update, Behavior Tree reaches the expected branch or task, path exists and movement follows the NavMesh, Perception or EQS events fire when relevant, logs stay clean, and multiplayer/server-authority assumptions are tested when relevant.\n- If AI possession, behavior-tree execution, perception/EQS, or path following cannot be observed, state AI behavior is unverified and do not claim the NPC loop works.\n\nIf MCP cannot inspect AI assets, navigation, or PIE, limit work to source/config/docs/log analysis and report the missing runtime evidence."));
    }

    FString MakeDataSaveAccessibilityDisciplineSkill()
    {
        return MakeSkillMarkdown(
            TEXT("unreal-data-save-accessibility-discipline"),
            TEXT("Plan SaveGame, data assets, localization, user settings, and accessibility changes with evidence."),
            TEXT("# Unreal Data Save Accessibility Discipline\n\nUse this before changing SaveGame classes or slot schema, GameInstance or subsystem persistence, Data Assets, Data Tables, Curve Tables, String Tables, localization keys, user settings, input remapping, accessibility options, subtitle or caption text, colorblind-safe UI, or other player-facing data/configuration. Persistent data and inclusive UX depend on clear ownership between source structs/classes, Blueprint assets, /Game data packages, config-backed settings, UI text, runtime save slots, migration/defaults, and platform assumptions.\n\nRequired discipline:\n- Run /unreal-tool-inventory, then inspect project settings, Content Browser state, relevant Data Assets/Data Tables/String Tables, SaveGame Blueprint or C++ class, GameInstance/subsystem owner, Widget Blueprint/UserWidget text, input/user settings, dirty packages, save slots or test fixtures when observable, localization/accessibility warnings, and recent logs.\n- State the data route: player-facing data result, owning schema/class/asset/table/slot, target /Game package path or config section, migration/default compatibility, localization namespace/key/table, accessibility requirement, narrow manage_asset/manage_inventory/manage_blueprint/system_control route plus any needed UI/editor route, validation, and rollback.\n- Keep authored data assets, Data Table row schemas, runtime SaveGame slots, GameUserSettings/config, localized display text, input remapping, subtitles/captions, color/contrast, font/DPI scale, controller navigation, and platform storage behavior distinct. Do not edit or move .uasset or .umap data assets with filesystem tools.\n- Ask before deleting or migrating save slots, changing persistent schema defaults, overwriting player/user settings, bulk text/key rewrites, removing localization keys, altering accessibility defaults, or changing platform storage paths.\n- After mutation, save touched assets/config, compile affected Blueprints or C++, refresh inspect, validate assets, run targeted PIE or automation, and capture exact report/log paths for migration, localization, accessibility, or settings checks.\n- Validate with evidence: Data Asset/Table rows read back correctly, SaveGame create/save/load/migration path works, defaults and existing slots remain compatible, localized text resolves to intended namespace/key/table, UI remains legible/focusable at target DPI/input modes, accessibility options can be toggled and persisted, config/user settings read back exactly, logs stay clean, and package/cook risk is recorded separately from editor-only evidence.\n- If MCP cannot inspect data assets, settings, UI text, save/load, localization, or accessibility state, state data or accessibility behavior is unverified and limit work to source/config/docs/log analysis."));
    }

    FString MakeSourceControlCollaborationDisciplineSkill()
    {
        return MakeSkillMarkdown(
            TEXT("unreal-source-control-collaboration-discipline"),
            TEXT("Plan Unreal package source-control work with checkout, submit, dependency, and team-safety evidence."),
            TEXT("# Unreal Source Control Collaboration Discipline\n\nUse this before checking out, submitting, bulk-moving, bulk-renaming, deleting, migrating, fixing redirectors, or changing shared Unreal packages, maps, external actors, World Partition cells, Data Layers, or source-control provider settings. Unreal package source control is not the same as local git: /Game assets, maps, redirectors, generated external actor packages, checkout locks, conflicts, and changelist descriptions must be handled through the editor and MCP evidence.\n\nRequired discipline:\n- Run /unreal-tool-inventory, then inspect Content Browser state, dirty packages, current map/external actor packages, selected assets, dependencies, redirectors, Developers folder scope, source-control provider availability, and recent Source Control, Message Log, and Output Log entries.\n- Before mutation, read source-control state for every affected package with manage_asset get_source_control_state when available; record exact /Game assetPaths, whether each asset exists, is current, canCheckout, canCheckIn, isCheckedOutOther, checkedOutBy, isConflicted, and whether a needed provider-enable action is missing from the live tool surface.\n- State the collaboration route: user intent, affected package list, dependency/redirector map, checkout or submit need, changelist/description text, approval requirement, narrow manage_asset source_control_checkout/source_control_submit/get_source_control_state route plus any needed asset/editor route, validation, and rollback or stop condition.\n- Keep local git state, Unreal source-control provider state, checked-out packages, dirty editor packages, map/external actor packages, redirectors, dependencies, generated files, and submitted changelists distinct. Do not use git add/commit/reset/checkout/clean, shell moves, or raw filesystem edits as a substitute for Unreal package source-control actions.\n- Ask before checkout that may lock assets for teammates, source_control_submit, provider changes, deleting or bulk-moving packages, fixing redirectors across shared folders, touching external actor packages, or continuing when any package is checked out by another user, conflicted, out of date, or not source controlled.\n- After mutation, save touched packages, refresh inspect, re-run get_source_control_state, validate dependencies and redirectors, capture exact submit/check-out result and log paths, and report which packages remain dirty, unchecked, conflicted, out of date, unsubmitted, or unverified.\n- If MCP cannot inspect source-control state or perform the needed checkout/submit route, state source-control behavior is unverified, name the missing capability, and stop before destructive package changes instead of falling back to local git or filesystem operations."));
    }

    FString MakePerformanceInsightsDisciplineSkill()
    {
        return MakeSkillMarkdown(
            TEXT("unreal-performance-insights-discipline"),
            TEXT("Plan Unreal performance, scalability, memory, and Insights trace work with measured evidence."),
            TEXT("# Unreal Performance And Insights Discipline\n\nUse this before claiming FPS, frame-time, hitch, memory, streaming, LOD, Nanite, texture streaming, scalability, rendering, load-time, cook-time, network, Slate/UI, asset-loading, or platform performance behavior. Treat Epic performance and Unreal Insights documentation as the measurement model, not proof of current editor state. Official anchors: Testing and Optimizing Your Content (https://dev.epicgames.com/documentation/en-us/unreal-engine/testing-and-optimizing-your-content), Unreal Insights (https://dev.epicgames.com/documentation/en-us/unreal-engine/unreal-insights-in-unreal-engine), and Scalability Reference (https://dev.epicgames.com/documentation/en-us/unreal-engine/scalability-reference-for-unreal-engine). A setting change is not proof; before/after measurement, trace or stat evidence, target hardware/context, and log/report paths are required.\n\nRequired discipline:\n- Run /unreal-tool-inventory, then inspect current project, map, PIE/editor state, target platform, scalability and device profile context, viewport/runtime scenario, content scope, memory stats, performance stats, scene stats, dirty packages, and recent performance, Insights, Message Log, and Output Log entries.\n- State the performance route: hypothesis, reproducible scenario, target budget, baseline metric, target hardware or editor mode, narrow system_control route for manage_performance actions such as start_profiling/stop_profiling/show_stats/generate_memory_report/set_scalability, narrow system_control route for manage_insights actions such as capture_insights_trace/get_trace_status/analyze_trace, validation, and rollback.\n- Keep FPS, frame time, game-thread, render-thread, GPU, RHI, memory, asset loading, shader compilation, streaming, networking, Slate/UI, cook/build time, scalability settings, device profiles, and visual quality distinct. Do not use a lower scalability level, reduced resolution, editor-only viewport state, or config diff as proof of optimization unless before/after evidence and user acceptance criteria support the tradeoff.\n- Ask before long profiling sessions, trace capture to network targets, changing scalability/device profile/config defaults, reducing quality, enabling expensive debug stats, clearing caches, rebuilding derived data, or running package/cook/build/deploy checks.\n- After measurement or mutation, stop profiling/tracing cleanly, refresh inspect, collect exact stat output, traceFile/utrace path, memory report path, automation or benchmark report path, screenshots when visual quality changed, and relevant log lines. Compare baseline and after values in the same scenario and state confidence limits.\n- Validate with evidence: target scenario reproduced, FPS/frame-time or thread bottleneck changed as expected, memory or streaming report supports the claim, Insights trace status/analysis is captured when used, scalability or console variable values read back, visual quality tradeoffs are accepted, logs stay clean, and package/cook/build performance is not inferred from editor-only profiling.\n- If MCP cannot inspect performance stats, run manage_performance actions, or capture/analyze Insights traces, state performance behavior is unverified, name the missing capability, and limit work to source/config/docs/log analysis."));
    }

    FString MakeSystemProjectDisciplineSkill()
    {
        return MakeSkillMarkdown(
            TEXT("unreal-system-project-discipline"),
            TEXT("Plan project settings, console/Python automation, profiling, packaging, cook, build, and platform operations with evidence."),
            TEXT("# Unreal System And Project Discipline\n\nUse this before changing Project Settings, Default Maps and Modes, GameInstance, input/platform/scalability/device settings, enabled plugins, modules, config-backed editor settings, console variables, console commands, Python scripts, Editor Utility workflows, profiling/stat capture, automation tests, packaging settings, cook/build/deploy settings, commandlet runs, source-control-facing project operations, or any editor/system operation that can affect the whole project. These changes depend on the current editor state, active map, config source, plugin/module load state, platform target, output logs, and explicit user approval for destructive or host-level actions.\n\nRequired discipline:\n- Run /unreal-tool-inventory, then inspect current project settings, maps and modes, enabled plugins/modules, config and scalability/device/platform state, PIE/editor state, dirty packages, source-control risk, Output Log and Message Log entries, profiling targets, automation status, and packaging/cook/build settings before acting.\n- State the system route: desired project-level result, exact setting or command target, owning config or editor subsystem, target platform, affected maps/assets, narrow system_control MCP route plus any needed inspect/control_editor route, approval needs, expected command output, validation, and rollback.\n- Keep project settings, console variables, console commands, Python/editor utility automation, profiling/stat capture, automation tests, packaging/cook/build/deploy operations, plugin/module enablement, source-control actions, and host shell commands distinct. Do not edit or move .uasset or .umap files directly, and do not use local shell commands for Unreal editor mutations when system_control or control_editor can perform them.\n- Ask before destructive, bulk, source-control, non-loopback/network, package/cook/build/deploy, host shell, or long-running operations. Do not bind services outside loopback unless the user explicitly opts in and token/security implications are named.\n- After mutation, save touched config/maps/assets when appropriate, refresh inspect, capture exact command output or log paths, review Output Log, Message Log, automation, profiler, packaging, cook, and build errors, and record warnings separately from blockers.\n- Validate with the strongest available evidence: settings read back from MCP/editor inspection, console variable values changed as intended, Python/editor utility scripts report successful scoped work, profiling captures include the requested stat or trace data, automation reports pass/fail counts, packaging/cook/build commands produce exact successful output, and runtime behavior is checked in editor or PIE when gameplay/editor behavior was affected.\n- If only config/source files were inspected, state packaging, cook, performance, editor automation, and runtime behavior are unverified. Do not claim packaging or performance from config-only evidence.\n\nIf MCP cannot inspect project settings or run system_control/control_editor operations, limit work to source/config/docs/log analysis and report the missing project-system evidence."));
    }
}
